export class Servicio{

  constructor(public categoria:string, public precio:number, public codigo:string, public nombre:string,
    public descripcion:string, public fechaRegistro:Date, public unidadMedida:string, public frecuencia:string){}
}
